# language: node_js

language: node_js
      node_js:
        - "node"
      before_script:
        - cd client
        - npm install
      cache:
        directories:
          - "node_modules"
      script:
        - npm test
